Var nb,resultat,somme,index1,index2: integer
Var Tab : Tableau
Début
    resultat = 0
    i = 0
    Afficher('Ecrire un entier')
    Recuperer nb
    nb = verificationINT(nb)
    Tant que nb !=0 ou resultat <=100:
        Tab[i] = nb
        i += 1
        resultat += nb
        Afficher('Ecricre un entier')
        Recuperer nb
        nb = verificationINT(nb)
    Fin tant que
    Afficher index()
Fin
    
Fonction verificationINT (nb:Entier)
    Tant que nb is not Entier
        Afficher ("Entrez un nombre correct ")
        Recuperer (nb)
    Fin tant que
    Retourner nb
Fin

Fonction index ():Entier
    Tab = []
    somme = 0
    Afficher ("Ecrire un entier pour le deuxieme index")
    Recuperer index2
    index2 = verificationINT(index2)
    while index1 > longueur(Tab)
        Afficher ("Ecrire un entier pour le deuxieme index")
        Recuperer index2
        index2 = verificationINT(index2)
    Fin tant que
    Afficher ("Ecrire un entier pour le premier index")
    Recuperer index1
    verificationINT(index1)
    while index1 > longueur(Tab)
        Afficher ("Ecrire un entier pour le premier index")
        Recuperer index1
        index1 = verificationINT(index1)
    Fin tant que
    somme = Tab[index1] + Tab[index2]
    Retourner somme
Fin